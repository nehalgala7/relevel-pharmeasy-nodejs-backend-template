const express = require("express");
const router = express.Router();
const MembershipController = require('../controllers/membership.controller');

// Create routes for membership here
module.exports = router;