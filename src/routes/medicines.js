const express = require("express");
const router = express.Router();
const MedicinesController = require('../controllers/medicines.controller');

// Create routes for medicines here
module.exports = router;