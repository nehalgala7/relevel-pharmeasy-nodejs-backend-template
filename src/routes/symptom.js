const express = require("express");
const router = express.Router();
const SymptomController = require('../controllers/symptom.controller');

// Create routes for symptom here
module.exports = router;